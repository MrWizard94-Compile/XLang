{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aether.local/schemas/aether-edit-v6.schema.json",
  "title": "Aether structural edit protocol v6",
  "description": "A bounded local structural edit request for a matching aether.ast/v6 document with shapes, dual-layout tables, and structured nurseries.",
  "type": "object",
  "additionalProperties": false,
  "required": ["protocol", "schema", "baseSource", "operations"],
  "properties": {
    "protocol": { "const": "aether.edit/v6" },
    "schema": { "const": "aether.ast/v6" },
    "baseSource": { "type": "string", "maxLength": 1000000 },
    "operations": { "type": "array", "minItems": 1, "maxItems": 32, "items": { "$ref": "#/$defs/operation" } }
  },
  "$defs": {
    "target": {
      "oneOf": [
        { "const": "world" },
        { "type": "string", "pattern": "^(record|shape|weave):[a-z][a-z0-9_]*$" }
      ]
    },
    "operation": {
      "oneOf": [
        {
          "type": "object", "additionalProperties": false,
          "required": ["op", "target", "declaration"],
          "properties": {
            "op": { "const": "replace" },
            "target": { "$ref": "#/$defs/target" },
            "declaration": { "$ref": "aether-ast-v6.schema.json#/$defs/editableDeclaration" }
          }
        },
        {
          "type": "object", "additionalProperties": false,
          "required": ["op", "target", "declaration"],
          "properties": {
            "op": { "const": "insertAfter" },
            "target": { "$ref": "#/$defs/target" },
            "declaration": { "$ref": "aether-ast-v6.schema.json#/$defs/editableDeclaration" }
          }
        },
        {
          "type": "object", "additionalProperties": false,
          "required": ["op", "target"],
          "properties": {
            "op": { "const": "delete" },
            "target": { "$ref": "#/$defs/target" }
          }
        }
      ]
    }
  }
}
