Act as a Principal Compiler Engineer and Programming Language Theorist.
I am designing a brand-new programming language. My goal is to combine the expressive readability of Python with the memory safety of Rust and the raw performance of C++, without relying on a slow garbage collector.
To kick off the project, I need you to write a zero-dependency Lexer and Abstract Syntax Tree (AST) parser from scratch in Rust.

1. Define the AST structures for basic language constructs (Variable declarations, Functions, Arithmetic loops, and an explicit Type System).
2. Write a Lexer that scans raw source string inputs and converts them into an array of distinct tokens.
3. Enforce a simple, ergonomic type-inference rule where variable types are checked at compile time.
   Provide the complete, clean, self-contained Rust code. Avoid placeholders or truncated code.
